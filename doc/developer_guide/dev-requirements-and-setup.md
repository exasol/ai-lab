# Requirements and Setup for Developers

## Requirements

This package requires:

* Python (>=3.10)
* Poetry (>=2.1.2)
* Docker
* AWS CLI

## Setup

### Install githooks

Run the following command before development.

```shell
cd githooks
bash install.sh
```
